source = ~/.config/hypr/env.conf
source = ~/.config/hypr/exec.conf
source = ~/.config/hypr/rules.conf

monitor=,preferred,auto,1

input {
  kb_layout = no
  repeat_delay = 250
  repeat_rate = 35
  touchpad {
    natural_scroll = yes
    tap-to-click = yes
  }
}

general {
  gaps_in = 8
  gaps_out = 16
  border_size = 3
  col.active_border = rgba(89b4faee) rgba(b4befeaa) 45deg
  col.inactive_border = rgba(313244aa)
  layout = dwindle
}

dwindle {
  smart_split = true
}

decoration {
  rounding = 10
  blur {
    enabled = true
    size = 6
    passes = 3
  }
}

animations {
  enabled = true
}

# Keybinds (Norwegian layout with Windows key)
$mod = SUPER
bind = $mod, Return, exec, wezterm
bind = $mod, Q, killactive
bind = $mod, D, exec, wofi --show drun
bind = $mod, E, togglesplit
bind = $mod, F, fullscreen, 1
bind = $mod, V, togglefloating
bind = $mod SHIFT, R, exec, hyprctl reload
bind = $mod, P, pseudo
bind = $mod, Space, layoutmsg, swapwithmaster

# Norwegian-specific keybinds
bind = $mod, semicolon, exec, wofi --show drun  # Norwegian ø key
bind = $mod, apostrophe, exec, wofi --show drun  # Norwegian æ key

# volume/brightness (PipeWire + brightnessctl)
bindle = , XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +5%
bindle = , XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -5%
bindle = , XF86AudioMute,        exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bindle = , XF86MonBrightnessUp,   exec, brightnessctl set +5%
bindle = , XF86MonBrightnessDown, exec, brightnessctl set 5%-

# screenshots
bind = $mod, S, exec, grim -g "$(slurp)" - | swappy -f -
bind = $mod SHIFT, S, exec, grim - | swappy -f -

# workspace switching
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

# move to workspace
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9
bind = $mod SHIFT, 0, movetoworkspace, 10
